<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Pokegem Details</title>
  </head>
  <body>
    <h1><%= pokegem.name %></h1>
    <img src="<%= pokegem.img %>" alt="<%= pokegem.name %>">
    <p>Series: <%= pokegem.series %></p>
    <p>ID: <%= pokegem._id %></p>
    <img src="<%= pokegem.cardImg %>" alt="<%= pokegem.name %>">
    <p>Price: <%= pokegem.price %></p>
    <a href="/pokegems/<%= pokegem._id %>/edit">Edit</a>
    <form action="/pokegems/<%= pokegem._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete</button>
    </form>
    <a href="/pokegems">Back to index</a>
    
  </body>
 
</html>
